@model int
@{

}
<td style="width:100%">
    <div class="btn-group" role="group">
        <a type="button" class="btn btn-primary" href="@Url.Action("Edit/"+Model)" ><i class="fas fa-edit"></i></a>
        <a type="button" class="btn btn-success" href="@Url.Action("Details/"+Model)"><i class="fas fa-clipboard"></i></a>
        <a type="button" class="btn btn-danger" href="@Url.Action("Delete/"+Model)" onclick="$('.top-alert').notify('Delete success', {position: 'top center'});" ><i class="fas fa-trash-alt"></i></a>
    </div>
    
</td>
